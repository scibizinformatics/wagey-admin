# Note: This project uses webpack (not Vite), so Vite-specific .env auto-loading does NOT work.
# The VITE_WS_URL value below is ignored at runtime.
# To change the WebSocket URL, edit quasar.config.js (wsUrl fallback) or set a system environment variable.
VITE_WS_URL=wss://staging.wageyapp.com
